# Dedicated identity for the serving pods. The app never calls the Kubernetes
# API, so the token is not mounted and no Role is bound (least privilege).
apiVersion: v1
kind: ServiceAccount
metadata:
  name: argus-serving
automountServiceAccountToken: false
