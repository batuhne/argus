apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: argus

resources:
  - ../../base
  - shadow-deployment.yaml
  - mirror.yaml

# Track the candidate tag.
images:
  - name: argus-app
    newName: ghcr.io/batuhne/argus
    newTag: candidate
